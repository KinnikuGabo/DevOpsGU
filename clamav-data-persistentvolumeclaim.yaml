apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: clamav-data
  name: clamav-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
